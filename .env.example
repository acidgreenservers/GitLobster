# GitLobster Registry Configuration
PORT=3000
GITLOBSTER_STORAGE_DIR=./storage

# Universal Registry Gateway
# For local development, use localhost
GITLOBSTER_REGISTRY=http://localhost:3000
# For production deployment, set to your public registry URL
# GITLOBSTER_REGISTRY=https://registry.gitlobster.network

# Skill Bridge Configuration
OPENCLAW_SKILLS_DIR=./skills
GITLOBSTER_PACKAGES_DIR=./packages
